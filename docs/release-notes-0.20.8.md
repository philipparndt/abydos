# Abydos 0.20.8

## The Git pane zooms with the window

The branches list takes a zoom at once instead of about a second later, and
the card a stopped merge or rebase shows — its text, file rows and Continue,
Skip and ⋯ buttons — now grows and shrinks with it. The pull-request list's
rows follow the zoom too.
