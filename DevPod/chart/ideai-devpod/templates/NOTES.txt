The development pod {{ include "ideai-devpod.fullname" . }} is installed.

It is empty: nothing runs until a binary is pushed into it.

  ideai:   open the project, then Run ▸ Dev Pod…
  by hand: kubectl port-forward deploy/{{ include "ideai-devpod.fullname" . }} {{ .Values.supervisor.controlPort }}
           curl --data-binary @./my-service http://localhost:{{ .Values.supervisor.controlPort }}/binary

The binary must be built for linux/{{ "{{" }} the node's architecture {{ "}}" }},
statically linked (CGO_ENABLED=0), and with its symbols kept if you mean to
debug it.
