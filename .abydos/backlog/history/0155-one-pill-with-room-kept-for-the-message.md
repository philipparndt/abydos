# One pill, with room kept for the message

`0462d09f3` · 2026-08-02

The message is back inside the run strip, in a fixed width that is there
whether or not anything is being said. Reserved rather than added: a
strip that grows when a run finishes moves the button somebody was about
to press, which was the reason for taking the message out in the first
place. And the toolbar draws a background behind every item it is given,
so two items for one control looked like two controls.

The cross that forgets a message stays; it is now inside the strip.
