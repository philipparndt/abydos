# The pictures are taken in the app's own palette

`123288a2e` · 2026-08-06

Fixed by `--theme` rather than by whatever the machine taking them is set to,
so the documentation shows the thing as it is meant to look and does not
change with somebody's preferences.
