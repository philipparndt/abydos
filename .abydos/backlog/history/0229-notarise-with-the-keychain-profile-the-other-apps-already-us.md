# Notarise with the keychain profile the other apps already use

`0a66fba09` · 2026-08-03

`notarytool` rather than a new one: it is the profile GoProfiler and
MQTT Analyzer are notarised with, and it is already in the keychain.
