# A diagram half way through being typed

The block below does not parse. It must not blank the document, and it must not
leave a gap where a picture would be: the code stays where it was written and the
complaint goes under it, naming the line of *this file*.

```mermaid
sequenceDiagram
    Customer->>Shop: Order a shelf
    Shop ??? Workshop
```

And the prose after it is still prose.
